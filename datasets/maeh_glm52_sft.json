[
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定供氧资源FIX-C-OXYGEN-02启动失败，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            if resource_id == \"FIX-C-OXYGEN-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定供氧资源FIX-C-OXYGEN-01输出参数异常，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            if resource_id == \"FIX-C-OXYGEN-01\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定燃油资源FIX-C-FUEL-02控制模块失效，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"燃油\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            resource = {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"燃油\",\n                \"mobility\": \"固定\",\n                \"zone\": zone,\n                \"current_location_id\": None,\n                \"capacity\": 1,\n            }\n            if resource_id == \"FIX-C-FUEL-02\":\n                resource[\"available\"] = False\n                resource[\"failure_reason\"] = \"控制模块失效\"\n            resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定燃油资源FIX-C-FUEL-01接口连接异常，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"燃油\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            if resource_id == \"FIX-C-FUEL-01\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"燃油\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定液压资源FIX-C-HYDRAULIC-02保护装置触发，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            if resource_id == \"FIX-C-HYDRAULIC-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定液压资源FIX-C-HYDRAULIC-01状态反馈中断，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            resource = {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"液压\",\n                \"mobility\": \"固定\",\n                \"zone\": zone,\n                \"current_location_id\": None,\n                \"capacity\": 1,\n            }\n            if resource_id == \"FIX-C-HYDRAULIC-01\":\n                resource[\"status\"] = \"feedback_interrupted\"\n                resource[\"available\"] = False\n            resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定供电资源FIX-C-POWER-02输出能力下降，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            if resource_id == \"FIX-C-POWER-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nC区固定供电资源FIX-C-POWER-01内部组件异常，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            if resource_id == \"FIX-C-POWER-01\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定供氧资源FIX-B-OXYGEN-02供给压力不足，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            if resource_id == \"FIX-B-OXYGEN-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定供氧资源FIX-B-OXYGEN-01通信链路中断，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            if resource_id == \"FIX-B-OXYGEN-01\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定燃油资源FIX-B-FUEL-02设备过载停机，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"燃油\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            if resource_id == \"FIX-B-FUEL-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"燃油\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定燃油资源FIX-B-FUEL-01执行机构失效，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"燃油\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_fuel_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定燃油资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定燃油资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-FUEL-{index:02d}\"\n            resource = {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"燃油\",\n                \"mobility\": \"固定\",\n                \"zone\": zone,\n                \"current_location_id\": None,\n                \"capacity\": 1,\n            }\n            if resource_id == \"FIX-B-FUEL-01\":\n                resource[\"status\"] = \"失效\"\n                resource[\"available\"] = False\n            else:\n                resource[\"status\"] = \"正常\"\n                resource[\"available\"] = True\n            resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定液压资源FIX-B-HYDRAULIC-02监测数据异常，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            if resource_id == \"FIX-B-HYDRAULIC-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定液压资源FIX-B-HYDRAULIC-01供给过程意外中止，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_hydraulic_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定液压资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定液压资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-HYDRAULIC-{index:02d}\"\n            if resource_id == \"FIX-B-HYDRAULIC-01\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"液压\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定供电资源FIX-B-POWER-02无法响应控制指令，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            if resource_id == \"FIX-B-POWER-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nB区固定供电资源FIX-B-POWER-01输出状态不稳定，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_power_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供电资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供电资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-POWER-{index:02d}\"\n            if resource_id == \"FIX-B-POWER-01\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供电\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\nA区固定供氧资源FIX-A-OXYGEN-02自检未通过，无法继续执行当前保障作业\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_fixed_oxygen_resources(\n    units_per_zone: int = 2,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化A、B、C三区当前可用的固定供氧资源。\n\n    输入：\n        units_per_zone（int）：\n            每个区域内固定供氧资源的数量。论文设置为2。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的固定供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if units_per_zone < 0:\n        raise ValueError(\"固定供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for zone in (\"A\", \"B\", \"C\"):\n        for index in range(1, units_per_zone + 1):\n            resource_id = f\"FIX-{zone}-OXYGEN-{index:02d}\"\n            if resource_id == \"FIX-A-OXYGEN-02\":\n                continue\n            resources.append(\n                {\n                    \"resource_id\": resource_id,\n                    \"resource_type\": \"供氧\",\n                    \"mobility\": \"固定\",\n                    \"zone\": zone,\n                    \"current_location_id\": None,\n                    \"capacity\": 1,\n                }\n            )\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供电资源MOB-POWER-05在第1项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供电\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供电\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-POWER-05\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failed_during_task\"] = 1\n        else:\n            resource[\"status\"] = \"available\"\n            resource[\"failed_during_task\"] = None\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供电资源MOB-POWER-06在第2项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供电\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供电\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-POWER-06\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failed_at_task\"] = 2\n            resource[\"current_location_id\"] = None\n        else:\n            resource[\"status\"] = \"available\"\n            resource[\"failed_at_task\"] = None\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供电资源MOB-POWER-07在第3项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供电\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供电\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if index == 7:\n            resource[\"status\"] = \"failed\"\n            resource[\"failure_task_index\"] = 3\n        else:\n            resource[\"status\"] = \"available\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供电资源MOB-POWER-08在第4项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供电\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_power_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供电资源。\n\n    输入：\n        resource_count（int）：\n            移动供电资源的数量。\n        initial_location_id（str）：\n            移动供电资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供电资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供电资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-POWER-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供电\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-POWER-08\":\n            resource[\"status\"] = \"失效\"\n            resource[\"failure_task_index\"] = 4\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动液压资源MOB-HYDRAULIC-05在第5项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"液压\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"液压\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-HYDRAULIC-05\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failed_at_task\"] = 5\n        else:\n            resource[\"status\"] = \"available\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动液压资源MOB-HYDRAULIC-06在第6项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"液压\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"液压\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-HYDRAULIC-06\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failure_task_index\"] = 6\n            resource[\"available\"] = False\n        else:\n            resource[\"status\"] = \"available\"\n            resource[\"available\"] = True\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动液压资源MOB-HYDRAULIC-07在第7项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"液压\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n\n        is_failed = False\n        if index == 7:\n            is_failed = True\n\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"液压\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n\n        if is_failed:\n            resource[\"status\"] = \"failed\"\n            resource[\"failure_task\"] = 7\n        else:\n            resource[\"status\"] = \"available\"\n\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动液压资源MOB-HYDRAULIC-08在第8项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"液压\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_hydraulic_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动液压资源。\n\n    输入：\n        resource_count（int）：\n            移动液压资源的数量。\n        initial_location_id（str）：\n            移动液压资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动液压资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动液压资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-HYDRAULIC-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"液压\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-HYDRAULIC-08\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failure_task_index\"] = 8\n            resource[\"available\"] = False\n        else:\n            resource[\"status\"] = \"available\"\n            resource[\"available\"] = True\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动燃油资源MOB-FUEL-05在第9项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"燃油\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"燃油\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-FUEL-05\":\n            resource[\"status\"] = \"失效\"\n        else:\n            resource[\"status\"] = \"可用\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动燃油资源MOB-FUEL-06在第10项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"燃油\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"燃油\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-FUEL-06\":\n            resource[\"status\"] = \"failed\"\n        else:\n            resource[\"status\"] = \"available\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动燃油资源MOB-FUEL-07在第11项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"燃油\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"燃油\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-FUEL-07\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failed_at_task\"] = 11\n        else:\n            resource[\"status\"] = \"available\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动燃油资源MOB-FUEL-08在第12项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"燃油\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_fuel_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动燃油资源。\n\n    输入：\n        resource_count（int）：\n            移动燃油资源的数量。\n        initial_location_id（str）：\n            移动燃油资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动燃油资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动燃油资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-FUEL-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"燃油\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-FUEL-08\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failed_at_task\"] = 12\n        else:\n            resource[\"status\"] = \"available\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供氧资源MOB-OXYGEN-05在第13项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供氧\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供氧\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-OXYGEN-05\":\n            resource[\"status\"] = \"失效\"\n            resource[\"failure_task_index\"] = 13\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供氧资源MOB-OXYGEN-06在第14项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供氧\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供氧\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-OXYGEN-06\":\n            resource[\"status\"] = \"失效\"\n            resource[\"failure_task\"] = 14\n            resource[\"available\"] = False\n        else:\n            resource[\"status\"] = \"正常\"\n            resource[\"available\"] = True\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供氧资源MOB-OXYGEN-07在第15项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供氧\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供氧\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-OXYGEN-07\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failure_task_index\"] = 15\n        else:\n            resource[\"status\"] = \"available\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动供氧资源MOB-OXYGEN-08在第16项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"供氧\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_mobile_oxygen_resources(\n    resource_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的移动供氧资源。\n\n    输入：\n        resource_count（int）：\n            移动供氧资源的数量。\n        initial_location_id（str）：\n            移动供氧资源的初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的移动供氧资源列表。\n\n    异常：\n        ValueError：\n            资源数量小于0时抛出。\n    \"\"\"\n\n    if resource_count < 0:\n        raise ValueError(\"移动供氧资源数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, resource_count + 1):\n        resource_id = f\"MOB-OXYGEN-{index:02d}\"\n        resource = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"供氧\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-OXYGEN-08\":\n            resource[\"status\"] = \"失效\"\n            resource[\"failure_context\"] = \"第16项保障任务执行期间失效\"\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n移动牵引车资源MOB-TOW-03在第17项保障任务执行期间失效\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_tow_tractors(\n    tow_tractor_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的牵引车。\n\n    输入：\n        tow_tractor_count（int）：\n            牵引车数量。\n        initial_location_id（str）：\n            牵引车初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的牵引车资源列表。\n\n    异常：\n        ValueError：\n            牵引车数量小于0时抛出。\n    \"\"\"\n\n    if tow_tractor_count < 0:\n        raise ValueError(\"牵引车数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, tow_tractor_count + 1):\n        resource_id = f\"MOB-TOW-{index:02d}\"\n        resources.append(\n            {\n                \"resource_id\": resource_id,\n                \"resource_type\": \"牵引车\",\n                \"mobility\": \"移动\",\n                \"zone\": None,\n                \"current_location_id\": initial_location_id,\n                \"capacity\": 1,\n            }\n        )\n    return resources\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_tow_tractors(\n    tow_tractor_count: int,\n    initial_location_id: str,\n) -> list[dict[str, Any]]:\n    \"\"\"初始化当前可用的牵引车。\n\n    输入：\n        tow_tractor_count（int）：\n            牵引车数量。\n        initial_location_id（str）：\n            牵引车初始位置编号。\n\n    输出：\n        list[dict[str, Any]]：\n            当前可用的牵引车资源列表。\n\n    异常：\n        ValueError：\n            牵引车数量小于0时抛出。\n    \"\"\"\n\n    if tow_tractor_count < 0:\n        raise ValueError(\"牵引车数量不能小于0\")\n\n    resources: list[dict[str, Any]] = []\n    for index in range(1, tow_tractor_count + 1):\n        resource_id = f\"MOB-TOW-{index:02d}\"\n        resource: dict[str, Any] = {\n            \"resource_id\": resource_id,\n            \"resource_type\": \"牵引车\",\n            \"mobility\": \"移动\",\n            \"zone\": None,\n            \"current_location_id\": initial_location_id,\n            \"capacity\": 1,\n        }\n        if resource_id == \"MOB-TOW-03\":\n            resource[\"status\"] = \"failed\"\n            resource[\"failed_during_task\"] = 17\n        resources.append(resource)\n    return resources"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R2因导航引导设备故障暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"available\": runway_id != \"R2\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R3因通信设备失效暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    runways = {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n    runways[\"R3\"][\"status\"] = \"closed\"\n    runways[\"R3\"][\"closure_reason\"] = \"通信设备失效\"\n    return runways"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R1因甲板作业车辆占道暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"available\": runway_id != \"R1\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R2因保障物资散落暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"关闭\" if runway_id == \"R2\" else \"可用\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R3因安全检查未通过暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"关闭\" if runway_id == \"R3\" else \"可用\",\n            \"closure_reason\": \"安全检查未通过\" if runway_id == \"R3\" else None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R1因跑道边界标志缺失暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"closed\" if runway_id == \"R1\" else \"open\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R2因监视设备异常暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"关闭\" if runway_id == \"R2\" else \"正常\",\n            \"status_reason\": \"监视设备异常\" if runway_id == \"R2\" else None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R3因气象条件临时变化暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"关闭\" if runway_id == \"R3\" else \"可用\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R1因消防设备异常暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"关闭\" if runway_id == \"R1\" else \"正常\",\n            \"closure_reason\": \"消防设备异常\" if runway_id == \"R1\" else None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R2因牵引作业占用暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    runways = {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n    runways[\"R2\"][\"status\"] = \"关闭\"\n    runways[\"R2\"][\"reason\"] = \"牵引作业占用\"\n\n    return runways"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R3因舰载机停留超时暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"available\": runway_id != \"R3\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R1因跑道入口被占用暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    runways = {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n    runways[\"R1\"][\"closed\"] = True\n    runways[\"R1\"][\"closure_reason\"] = \"跑道入口被占用\"\n    return runways"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R2因跑道末端区域受限暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"available\": False if runway_id == \"R2\" else True,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R3因异物检测系统告警暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    runways = {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n    runways[\"R3\"][\"status\"] = \"closed\"\n    runways[\"R3\"][\"closure_reason\"] = \"异物检测系统告警\"\n    return runways"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R1因紧急保障任务占用暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n            \"status\": \"关闭\" if runway_id == \"R1\" else \"可用\",\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R2因地面引导信号中断暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    runways = {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n    runways[\"R2\"][\"status\"] = \"关闭\"\n    runways[\"R2\"][\"closure_reason\"] = \"地面引导信号中断\"\n\n    return runways"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n起飞跑道R3因运行状态信息缺失暂时关闭\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    return {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_runways() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化3条起飞跑道。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        包含R1、R2和R3的起飞跑道字典。\n    \"\"\"\n\n    runways = {\n        runway_id: {\n            \"location_id\": runway_id,\n            \"kind\": \"起飞跑道\",\n            \"zone\": None,\n        }\n        for runway_id in (\"R1\", \"R2\", \"R3\")\n    }\n\n    runways[\"R3\"][\"status\"] = \"closed\"\n\n    return runways"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A1至起飞跑道R1的路径SEG-A1-R1因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n    if start_location_id == \"A1\" and end_location_id == \"R1\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A2至起飞跑道R2的路径SEG-A2-R2因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    if segment_id == \"SEG-A2-R2\":\n        return {\n            \"segment_id\": segment_id,\n            \"start_location_id\": start_location_id,\n            \"end_location_id\": end_location_id,\n            \"distance\": float(distance),\n            \"status\": \"不可通行\",\n            \"blocked_reason\": \"通道异常\",\n        }\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A3至起飞跑道R3的路径SEG-A3-R3因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n\n    status = \"可通行\"\n    blocked_reason = None\n\n    if start_location_id == \"A3\" and end_location_id == \"R3\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A4至起飞跑道R1的路径SEG-A4-R1因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    segment = {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n    if segment_id == \"SEG-A4-R1\":\n        segment[\"status\"] = \"不可通行\"\n        segment[\"blocked_reason\"] = \"通道异常\"\n\n    return segment"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A5至起飞跑道R2的路径SEG-A5-R2因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n    if segment_id == \"SEG-A5-R2\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A6至起飞跑道R3的路径SEG-A6-R3因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n    if segment_id == \"SEG-A6-R3\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B1至起飞跑道R1的路径SEG-B1-R1因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    segment: dict[str, Any] = {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n    if segment_id == \"SEG-B1-R1\":\n        segment[\"status\"] = \"不可通行\"\n        segment[\"blocked_reason\"] = \"通道异常\"\n    return segment"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B2至起飞跑道R2的路径SEG-B2-R2因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n    if segment_id == \"SEG-B2-R2\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B3至起飞跑道R3的路径SEG-B3-R3因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n\n    status = \"可通行\"\n    blocked_reason = None\n\n    if segment_id == \"SEG-B3-R3\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B4至起飞跑道R1的路径SEG-B4-R1因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    blocked_reason = None\n    if start_location_id == \"B4\" and end_location_id == \"R1\":\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\" if blocked_reason is None else \"不可通行\",\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B5至起飞跑道R2的路径SEG-B5-R2因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    result = {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n    if segment_id == \"SEG-B5-R2\":\n        result[\"status\"] = \"不可通行\"\n        result[\"blocked_reason\"] = \"通道异常\"\n    return result"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B6至起飞跑道R3的路径SEG-B6-R3因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n\n    status = \"可通行\"\n    blocked_reason = None\n\n    if start_location_id == \"B6\" and end_location_id == \"R3\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B7至起飞跑道R1的路径SEG-B7-R1因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n\n    status = \"可通行\"\n    blocked_reason = None\n\n    if segment_id == \"SEG-B7-R1\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B8至起飞跑道R2的路径SEG-B8-R2因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n    if segment_id == \"SEG-B8-R2\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C1至起飞跑道R3的路径SEG-C1-R3因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n    if segment_id == \"SEG-C1-R3\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C2至起飞跑道R1的路径SEG-C2-R1因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n\n    status = \"可通行\"\n    blocked_reason = None\n\n    if segment_id == \"SEG-C2-R1\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C3至起飞跑道R2的路径SEG-C3-R2因通道异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": \"可通行\",\n        \"blocked_reason\": None,\n    }\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef create_route_segment(\n    start_location_id: str,\n    end_location_id: str,\n    distance: float = 1.0,\n) -> dict[str, Any]:\n    \"\"\"创建一条单向路径段。\n\n    输入：\n        start_location_id（str）：\n            路径起点编号。\n        end_location_id（str）：\n            路径终点编号。\n        distance（float）：\n            路径距离，默认值为1.0。\n\n    输出：\n        dict[str, Any]：\n        包含路径编号、起点、终点、距离和通行状态的字典。\n\n    异常：\n        ValueError：\n            起点与终点相同，或者距离不大于0时抛出。\n    \"\"\"\n\n    if start_location_id == end_location_id:\n        raise ValueError(\"路径段的起点和终点不能相同\")\n    if distance <= 0:\n        raise ValueError(\"路径距离必须大于0\")\n\n    segment_id = f\"SEG-{start_location_id}-{end_location_id}\"\n    status = \"可通行\"\n    blocked_reason = None\n\n    if segment_id == \"SEG-C3-R2\":\n        status = \"不可通行\"\n        blocked_reason = \"通道异常\"\n\n    return {\n        \"segment_id\": segment_id,\n        \"start_location_id\": start_location_id,\n        \"end_location_id\": end_location_id,\n        \"distance\": float(distance),\n        \"status\": status,\n        \"blocked_reason\": blocked_reason,\n    }"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC01计划于08:00到达，现延迟2分钟，预计于08:02到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = planned_arrival_time + (2 if aircraft_id == \"AC01\" else 0)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(actual_arrival_time),\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC02计划于08:03到达，现延迟6分钟，预计于08:09到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC02\":\n            actual_arrival_time = float(planned_arrival_time + 6)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC03计划于08:06到达，现延迟13分钟，预计于08:19到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC03\":\n            actual_arrival_time = float(8 * 60 + 19)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC04计划于08:09到达，现延迟5分钟，预计于08:14到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC04\":\n            actual_arrival_time = float(planned_arrival_time + 5)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC05计划于08:12到达，现延迟11分钟，预计于08:23到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC05\":\n            actual_arrival_time = float(8 * 60 + 23)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC06计划于08:15到达，现延迟5分钟，预计于08:20到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = planned_arrival_time\n        if aircraft_id == \"AC06\":\n            actual_arrival_time = planned_arrival_time + 5\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(actual_arrival_time),\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC07计划于08:18到达，现延迟3分钟，预计于08:21到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC07\":\n            actual_arrival_time = float(planned_arrival_time + 3)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC08计划于08:21到达，现延迟9分钟，预计于08:30到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC08\":\n            actual_arrival_time = 8 * 60 + 30\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC09计划于08:24到达，现延迟4分钟，预计于08:28到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC09\":\n            actual_arrival_time = float(planned_arrival_time + 4)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC10计划于08:27到达，现延迟7分钟，预计于08:34到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC10\":\n            actual_arrival_time = float(planned_arrival_time + 7)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC11计划于08:30到达，现延迟11分钟，预计于08:41到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC11\":\n            actual_arrival_time = 8 * 60 + 41\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC12计划于08:33到达，现延迟14分钟，预计于08:47到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC12\":\n            actual_arrival_time = float(planned_arrival_time + 14)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC13计划于08:36到达，现延迟3分钟，预计于08:39到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC13\":\n            actual_arrival_time = float(planned_arrival_time + 3)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC14计划于08:39到达，现延迟12分钟，预计于08:51到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC14\":\n            actual_arrival_time = 8 * 60 + 51\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC15计划于08:42到达，现延迟8分钟，预计于08:50到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC15\":\n            actual_arrival_time = float(planned_arrival_time + 8)\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n舰载机AC16计划于08:45到达，现延迟10分钟，预计于08:55到达\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": float(planned_arrival_time),\n        }\n    return aircraft\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_aircraft(\n    aircraft_count: int,\n    support_stand_ids: Sequence[str],\n) -> dict[str, dict[str, Any]]:\n    \"\"\"初始化舰载机并依次放置在给定保障站位。\n\n    输入：\n        aircraft_count（int）：\n            需要初始化的舰载机数量。\n        support_stand_ids（Sequence[str]）：\n            可供舰载机初始停放的保障站位编号序列。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        以舰载机编号为键的舰载机状态字典。\n\n    异常：\n        ValueError：\n            舰载机数量小于0，或者超过给定保障站位数量时抛出。\n    \"\"\"\n\n    if aircraft_count < 0:\n        raise ValueError(\"aircraft_count不能小于0\")\n    if aircraft_count > len(support_stand_ids):\n        raise ValueError(\"舰载机数量不能超过可用保障站位数量\")\n\n    aircraft: dict[str, dict[str, Any]] = {}\n    for index in range(1, aircraft_count + 1):\n        aircraft_id = f\"AC{index:02d}\"\n        planned_arrival_time = 8 * 60 + (index - 1) * 3\n        actual_arrival_time = float(planned_arrival_time)\n        if aircraft_id == \"AC16\":\n            actual_arrival_time = 8 * 60 + 55\n        aircraft[aircraft_id] = {\n            \"aircraft_id\": aircraft_id,\n            \"location_id\": support_stand_ids[index - 1],\n            \"planned_arrival_time\": float(planned_arrival_time),\n            \"actual_arrival_time\": actual_arrival_time,\n        }\n    return aircraft"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B8因系留装置异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    if \"B8\" in stands:\n        stands[\"B8\"][\"status\"] = \"unavailable\"\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C1因供电接口损坏无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stand_info: dict[str, Any] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n            if stand_id == \"C1\":\n                stand_info[\"available\"] = False\n                stand_info[\"unavailable_reason\"] = \"供电接口损坏\"\n            stands[stand_id] = stand_info\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C2因液压接口异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    stands[\"C2\"][\"status\"] = \"液压接口异常\"\n    stands[\"C2\"][\"available\"] = False\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C3因燃油接口失效无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    stands[\"C3\"][\"fuel_interface\"] = \"failed\"\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C4因供氧接口异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    stands[\"C4\"][\"available\"] = False\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位C5因安全区域被占用无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            if stand_id == \"C5\":\n                continue\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A1因地面标志缺失无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stand_info = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n            if stand_id == \"A1\":\n                stand_info[\"available\"] = False\n                stand_info[\"unavailable_reason\"] = \"地面标志缺失\"\n            stands[stand_id] = stand_info\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A2因保障设备无法接入无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stand_info: dict[str, Any] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n            if stand_id == \"A2\":\n                stand_info[\"available\"] = False\n            stands[stand_id] = stand_info\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A3因作业空间受到限制无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            if stand_id == \"A3\":\n                continue\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A4因停放状态检测异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            if stand_id == \"A4\":\n                continue\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A5因防护装置失效无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stand_info = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n            if stand_id == \"A5\":\n                stand_info[\"status\"] = \"防护装置失效\"\n                stand_info[\"available\"] = False\n            stands[stand_id] = stand_info\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位A6因通信接口中断无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stand_info = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n            if stand_id == \"A6\":\n                stand_info[\"status\"] = \"通信接口中断\"\n                stand_info[\"available\"] = False\n            stands[stand_id] = stand_info\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B1因牵引通道被占用无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    stands[\"B1\"][\"available\"] = False\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B2因邻近区域存在冲突无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    stands.pop(\"B2\", None)\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B3因保障条件不满足无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            if stand_id == \"B3\":\n                continue\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands"
  },
  {
    "system": "\n你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。\n\n必须严格遵守以下规则，违反任意一项均视为修复失败。\n\n【任务目标】\n1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。\n2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。\n3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。\n\n【修改范围】\n1. 只能修改已有函数的函数体，不得进行以下操作：\n   - 新增或删除函数；\n   - 修改函数名称；\n   - 修改函数参数及其顺序；\n   - 修改函数返回值的数据类型或结构；\n   - 修改与当前特情无关的函数。\n\n2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。\n\n【修复要求】\n1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。\n2. 严禁采用以下方式绕过问题：\n   - 使用 return、pass等语句跳过核心逻辑；\n   - 硬编码调度结果或资源编号；\n   - 删除关键调度步骤或约束检查；\n   - 捕获异常后忽略冲突或返回默认结果；\n   - 通过固定条件使受影响代码不再执行。\n\n【事实约束】\n1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。\n2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。\n3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。\n\n【输出要求】\n1. 仅输出修改后的完整Python代码。\n2. 输出必须包含原始代码中的全部import语句和所有函数。\n3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。\n4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。\n",
    "instruction": "\nEMERGENCY_SITUATIONS:\n保障站位B4因站位状态信息异常无法使用\n\nCODE:\nfrom __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stands[stand_id] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n    return stands\n\n请基于上述突发事件，对代码进行必要修改。\n",
    "input": "",
    "output": "from __future__ import annotations\nfrom collections import Counter\nfrom copy import deepcopy\nfrom typing import Any, Iterable, Mapping, Sequence\n\ndef initialize_support_stands() -> dict[str, dict[str, Any]]:\n    \"\"\"初始化A、B、C三区共19个保障站位。\n\n    输入：\n        无。\n\n    输出：\n        dict[str, dict[str, Any]]：\n        A区6个、B区8个、C区5个保障站位组成的字典。\n    \"\"\"\n\n    stand_counts = {\"A\": 6, \"B\": 8, \"C\": 5}\n    stands: dict[str, dict[str, Any]] = {}\n    for zone, count in stand_counts.items():\n        for index in range(1, count + 1):\n            stand_id = f\"{zone}{index}\"\n            stand_info: dict[str, Any] = {\n                \"location_id\": stand_id,\n                \"kind\": \"保障站位\",\n                \"zone\": zone,\n            }\n            if stand_id == \"B4\":\n                stand_info[\"status\"] = \"异常\"\n                stand_info[\"available\"] = False\n            stands[stand_id] = stand_info\n    return stands"
  }
]
