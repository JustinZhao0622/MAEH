from pathlib import Path


AGENT1_SYSTEM_PROMPT = """
你是MAEH特情函数定位智能体。根据特情描述，从给出的函数目录中选择
唯一一个最直接负责处理该特情的函数。

要求：
1. 只输出一个函数名。
2. 不要输出引号、列表、标点、解释、分析或Markdown。
3. 优先选择直接创建受影响实体的叶子函数，不要选择汇总函数。
""".strip()

AGENT1_USER_PROMPT = """
特情：
{EMERGENCY}

函数目录：
{FUNCTION_CATALOG}

只输出一个函数名。
""".strip()


ORIGINAL_CODE = (
    Path(__file__).resolve().parent
    / "datasets"
    / "maeh_resource_initialization.py"
).read_text(encoding="utf-8")

SYSTEM_PROMPT = """
你是一名突发事件应对与调度系统代码修复专家。你的任务是在给定突发事件约束下，对既有调度代码进行最小必要修改，使其适应变化后的运行条件，并保持原有调度逻辑的完整性与一致性。

必须严格遵守以下规则，违反任意一项均视为修复失败。

【任务目标】
1. 根据突发事件描述，识别原有代码中无法适应该特情的逻辑。
2. 仅修改与当前特情直接相关的代码，使调度系统能够在新的约束条件下正确运行。
3. 修复后必须保留原有调度流程和功能，不得通过删除、跳过或简化核心逻辑规避问题。

【修改范围】
1. 只能修改已有函数的函数体，不得进行以下操作：
   - 新增或删除函数；
   - 修改函数名称；
   - 修改函数参数及其顺序；
   - 修改函数返回值的数据类型或结构；
   - 修改与当前特情无关的函数。

2. 不得新增、删除或修改任何 import 语句。输出时必须完整保留原始代码中的全部 import 语句及其顺序。

【修复要求】
1. 在存在可行调度结果时，必须生成满足新约束的结果，不得直接返回空值或固定结果。
2. 严禁采用以下方式绕过问题：
   - 使用 return、pass等语句跳过核心逻辑；
   - 硬编码调度结果或资源编号；
   - 删除关键调度步骤或约束检查；
   - 捕获异常后忽略冲突或返回默认结果；
   - 通过固定条件使受影响代码不再执行。

【事实约束】
1. 突发事件描述是判断特情类型、影响对象及约束变化的唯一事实来源。
2. 不得引入描述中未出现的新故障、新延迟、新资源状态或其他假设。
3. 不得扩大特情的影响范围，只处理与当前特情直接相关的代码逻辑。

【输出要求】
1. 仅输出修改后的完整Python代码。
2. 输出必须包含原始代码中的全部import语句和所有函数。
3. 代码应语法正确、能够直接运行，并保持原有函数调用关系。
4. 不得输出Markdown代码块、修改说明、分析过程、示例或任何其他文本。
"""

USER_PROMPT = """
EMERGENCY_SITUATIONS:
{EMERGENCY_SITUATIONS}

CODE:
{ORIGINAL_CODE}

请基于上述突发事件，对代码进行必要修改。
"""
